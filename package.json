{
  "name": "royalty-sync-service",
  "version": "0.1.0",
  "private": true,
  "description": "WellnessLiving -> GoHighLevel -> Supabase sync service (HRRAFEBAV)",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "bin": {
    "royalty-sync": "./dist/cli/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write \"src/**/*.ts\" \"api/**/*.ts\" \"tests/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"api/**/*.ts\" \"tests/**/*.ts\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "verify": "npm run format:check && npm run lint && npm run typecheck && npm run test",
    "start": "node --env-file-if-exists=.env dist/cli/main.js",
    "cli:dev": "npm run --silent build && node --env-file-if-exists=.env dist/cli/main.js",
    "healthcheck": "npm run --silent cli:dev -- healthcheck"
  },
  "dependencies": {
    "@types/nodemailer": "^8.0.1",
    "nodemailer": "^9.1.0",
    "zod": "^3.23.8"
  },
  "overrides": {
    "glob": "^13.0.6"
  },
  "optionalDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.658.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.11.1",
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^2.1.2",
    "eslint": "^9.11.1",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.8.0",
    "vitest": "^2.1.2"
  }
}
